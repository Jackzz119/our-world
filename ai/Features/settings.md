# Settings · 设置系统 系统设计文档

> 最后更新：2026-07-06（占位创建，待正式开工）
> 路线图位置：暂无（用户指定先建档留着，退出账号功能落此处）
> 关联：`.claude/skills/ux/decisions.md` D-4（退出三层体系）、`src/themes/cinnaglass/settings.tsx`（设置弹窗 UI 已复刻，纯 mock）

---

## 一、功能目标

把已复刻的设置弹窗 UI 接上真实能力。目前明确要归入设置的：

1. **退出账号 / 换账号**（D-4 第三层）：登出 auth 会话回登录页；换账号 = 登出 + 重新登录
2. （待陆续归集）通知、主题切换（多 theme 落地后）、隐私等

**明确不归设置的**（见 ux decisions.md D-4）：
- 退出场景（房间）→ 场景内悬浮 UI
- 退出世界 → 世界层菜单（Discord「Leave Server」语义）

## 二、调用链路

（待设计：`supabase.auth.signOut()` → 清本地态 → 回 `/login`）

## 三、模块设计

（待设计）

## 四、时间线 / 参数

（待定）

## 五、待实现 / 已知问题

- 设置弹窗现为纯 mock（`settings.tsx`），所有开关无后端
- 登出后 dev 自动登录开关（`VITE_DEV` 凭据）的交互需要想清楚：dev 模式登出会不会立刻被自动登回

## 实现计划

进度：0 / 0 subtasks 完成（0%）

## 测试记录

（无）
