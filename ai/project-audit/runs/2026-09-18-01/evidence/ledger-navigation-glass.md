# Ledger — ai/Features/navigation-glass.md

## 体积

| | bytes | 行 |
| --- | --- | --- |
| 原文 | 2660 | 24 |
| 压缩稿 | 2613 | 24 |
| 目标（≤2/3） | 1773 | — |
| 实际比例 | **98.2%** | — |

**target_met = false，exception = true。** 原文已是高密度功能文档，无流水段落可删（project-audit 的 design-review 亦将其列为「保留」）。详见文末「例外说明」。

## 入向引用与锚点

全仓 grep `navigation-glass.md#`：**零命中**，无锚点引用，标题可自由改写。

纯文件名引用：`ai/PROJECT.md:88`、`ai/PROJECT.md:174`、`ai/TODO.md:103`、`ai/design_system/uiux/uiux.md:14`、`ai/design_system/uiux/cinnaglass/ui-system.md:13`、`ai/design_system/uiux/cinnaglass/decisions.md:9`、`.../journal-room-object/navigation-implementation.md:87`、`ai/Features/ui-system/ui-system.md §2.1`。

出站链接全部保留且经磁盘核实存在：`ui-system/ui-system.md`、`../design_system/uiux/cinnaglass/journal-room-object/room-journal-concept.png`、`.../navigation-implementation.md`。

## 保留清单

| 保留项 | 类型 |
| --- | --- |
| 2026-09-07 实现 / 2026-09-11 复核；按用户批准的「桌上的同一本日记」稿，该轮只做导航 | 决策背景 |
| **用户特别认可图标发光，覆盖原提案「压低光晕」的建议** | 独有决策及理由 |
| 2026-09-11 起为全项目 **A 类场景悬浮 UI 基准**；B / C 类边界见 UI 统一计划，本文不另建任务列表 | 强制规则 + 职责边界 |
| NG-1 ~ NG-4 四条完成状态（含「用户已认可」「书本静态轮重新回归通过」「**未部署**」） | 验收条件 / 状态 |
| 范围：`shell/rail.tsx` + 导航专属 CSS / 图标 / 纹理 + 只读验证脚本；房间/工具小菜单算在内 | 范围边界 |
| **不改**日记与设置等功能弹窗、不改共用旧材质变量、不改场景暂停与后端 | 强制规则 |
| 用户批准设计板链接；验证记录一律归该设计目录，避免再生一份样式真源 | 强制规则 |
| 机制：纹理是生成后固定保存的 512×512 透明图（`frost.webp`），CSS 固定取样，**无运行时随机** | 机制权威事实 |
| 机制：实时透景用 `backdrop-filter`；边缘是**固定角度 `conic-gradient` + mask 模拟**，颜色随 mood 预设，**不算真实灯光反射，不按时辰换导航图片** | 机制权威事实 |
| 机制：只有房间底图有三档图片与 Pixi 交叉淡化 | 对比澄清 |
| 机制：天气只间接改变透过去的场景，**当前不直接驱动导航边光** | 机制权威事实 |
| 行为：桌面竖条五主按钮 / 窄屏矮屏底部横条；参数收敛在 `navigation-glass.css`，不覆盖全局纸色 | 实现事实 |
| 行为：菜单用同材质更厚遮光版 + 暖色滑块；点击走原有回调，不新增后端写入 | 实现事实 |
| 行为：触屏按住由指针状态驱动，松手 / 取消 / 移出即清除；点外或 Escape 收起菜单 | 交互规则 |
| Vite 与改动文件 ESLint 通过；**完整类型构建仍受既有聊天 `Msg` 导入错误阻断**；日记与其他弹窗未迁移 | 已知缺口 |

## 删除清单

| 原文位置 | 摘句 | 理由 |
| --- | --- | --- |
| :20 | 「桌面导航 **74×384**，五个主按钮；窄屏/矮屏为 **292×62** 底部横条」 | 尺寸数值与 `navigation-implementation.md` 重复（该文是数值权威），按分工只留形态描述，数值交末段链接一句带过 |
| :22 | 「CSS 固定按 **256×512、42% 50%** 取样」 | 同上，属数值层；改为「CSS 按固定尺寸与偏移取样」，保住「固定 / 无运行时随机」这个机制结论 |
| :3 | 「实施轮明确只实现导航」与 :14「不改日记、设置等功能弹窗」 | 两处同义，合并为一处（抬头保留「该轮只做导航」，范围段保留具体不改清单） |
| :5 | 「跨组件迁移、B 类任务界面与 C 类专属物件的边界」 | 压成「迁移范围与 B / C 类边界」，指向同一文档 |
| :16 | 「最终验证记录归该设计目录」与 :24「逐项验证与未覆盖项见实现记录」 | 两句同指一处，抬头改为「验证记录一律归该设计目录」，末段收敛为一句「本文不复述」 |
| :24 | 「布局/材质数值、生成来源、逐项验证与未覆盖项」 | 与删掉的行内数值合并成一句「尺寸、材质数值、生成来源与逐项验证见实现记录（本文不复述）」 |
| 各处 | 逗号句式、重复主语（「导航自己的」「点击入口仍走」等） | 纯句式压缩，无信息损失 |

## 改正清单

本轮**未发现陈旧陈述**。逐项核对结果：

| 原陈述 | 核对结果 | 依据 |
| --- | --- | --- |
| 纹理 `public/ui/nav/frost.webp` | 存在，298164 bytes | `ls -la public/ui/nav/` |
| 512×512 生成纹理 | 生成脚本 `.resize(512, 512)` → `.webp({quality:90, alphaQuality:100})` | `scripts/pack-navigation-texture.mjs:8,23-24`（经 project-audit evidence 复核） |
| CSS 固定取样 | `background: url('/ui/nav/frost.webp') 42% 50% / 256px 512px;` — 固定值，无随机 | `src/themes/cinnaglass/shell/navigation-glass.css:91` |
| `backdrop-filter` 实时透景 | `backdrop-filter: blur(var(--nav-blur)) saturate(0.7);` | 同文件 `:62-63` |
| `conic-gradient` 边光 | `background: conic-gradient(` | 同文件 `:103` |
| 桌面 74×384 | `width: 74px; height: 384px;` | 同文件 `:76-77` |
| 窄屏 292×62 | `width: 292px; ... height: 62px;`（媒体查询内） | 同文件 `:444-446` |
| 五个主按钮 | `rail.tsx:109-114` 主层按钮 rooms / chat / music / modules / settings 恰五个 | project-audit evidence 复核 |
| 「按 mood 预设变化」 | `--nav-frost` 等按档变量 | `navigation-glass.css:10,27` |

## 待核清单

- 「书本静态实施轮重新回归通过，未部署」：为 2026-09-07 当时的回归结论，本轮未重跑 `scripts/check-navigation.mjs`。
- 「完整类型构建仍受既有聊天 `Msg` 导入错误阻断」：本轮未执行 `tsc`，按原文保留（多份实现记录有同一表述）。
- 「三时辰、四尺寸、键盘/触屏模拟」验证证据在 `.../journal-room-object/navigation-verification/`（目录存在，32 项），未逐张核。

## 例外说明

**做不到 ≤1773 bytes，停在 2613（98.2%）。** 逐段理由：

1. **抬头（约 330B）** — 含本文唯一记载的用户裁决（图标发光覆盖「压低光晕」提案）与「A 类基准」的地位声明，两者都是其他 UI 文档回指本文的原因，不可删。
2. **NG-1 ~ NG-4（约 450B）** — 功能文档的完成情况表，是任务侧的状态真源之一；已做句式压缩（去掉冒号与重复连接词），条目本身不能合并。
3. **范围段（约 350B）** — 「不改什么」是这轮实施的硬边界，后续迁移轮要照抄；删掉就要在 UI 统一计划里重建。
4. **机制澄清（约 620B）** — 任务与 project-audit 均指定本节为机制事实的**权威正文**（ui-system.md §2.1 的六行复述将改为引用本文）。这里的每一条都是否定式澄清（不是运行时随机 / 不是真实反射 / 不按时辰换图 / 天气不直接驱动），删任一条就会让被它纠正的误解重新成立。
5. **实现摘要 + 末段（约 700B）** — 已把两处尺寸数值移交 `navigation-implementation.md`，剩下的是交互因果（触屏按住→指针状态清除、点外/Escape 收起）与已知缺口（未部署、类型构建阻断、其他弹窗未迁移），都是状态信息。

本文原就是 24 行、无日期流水、无作废方案的功能文档，可压缩的只有重复数值与句式；要到 2/3 必须整节删掉「实现摘要」或「NG 状态表」，二者分别是机制/行为事实与验收状态，故选择保真优先。
