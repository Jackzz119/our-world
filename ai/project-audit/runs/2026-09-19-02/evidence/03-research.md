# 03 · 调研表（第三步真实调研，检索日期 2026-09-19）

按技能要求逐条记录：查询 | 检索日期 | 来源标题和 URL | 来源版本 | 可核验的结论 | 适用前提 | 本项目证据 | 采纳/不采纳及理由。
本机安装版本（`node_modules/.pnpm`）：`@supabase/supabase-js` 2.98.0（auth-js / realtime-js / storage-js / postgrest-js 同版），`pixi.js` 8.19.0，`react` 19.2.4，`react-router-dom` 7.13.1。「本项目证据」中标 **待分区核** 的，由 03-S03～S06 的分区 review 落实。

| # | 查询 | 来源（标题 / URL） | 来源版本 | 可核验的结论 | 适用前提 | 本项目证据 | 采纳 / 不采纳 |
|---|---|---|---|---|---|---|---|
| R1 | Supabase Realtime "Broadcast from Database" 契约 | Broadcast · https://supabase.com/docs/guides/realtime/broadcast | 在线文档，无版本日期 | 两个触发函数：`realtime.send()`（任意 payload）与 `realtime.broadcast_changes(topic, event, operation, table, schema, NEW, OLD)`；数据库广播**默认私有**，客户端须 `channel(name, { config: { private: true } })` 且先 `supabase.realtime.setAuth()`；文档**未给出顺序/投递保证**；消息按日分区保留 72h～4d（供 Replay） | 使用 DB 触发器广播的项目 | `ai/features/chat.md` §二投递模型与之一致；payload 字段名与 `use-world-stream.ts` 假设是否一致 **待分区 C 核** | 采纳为契约基准；「无投递保证」→ 断线漏消息必须由应用层补拉（见 R2） |
| R2 | Broadcast Replay 配置 | Broadcast（同上）+ Realtime: Broadcast Replay · https://supabase.com/blog/realtime-broadcast-replay | 在线文档 | `config.broadcast.replay = { since: <epoch ms>, limit?: ≤25 }`，仅私有通道、仅 DB 广播的消息可回放 | realtime-js 支持该选项 | 本机 realtime-js 2.98 `RealtimeChannel.d.ts:7-21` 已有 `ReplayOption { since; limit? }`——可用 | 采纳为建议：订阅时带 `replay.since = 上次收到的 created_at`；但 `limit ≤ 25`，长时间离线仍需按游标 refetch 兜底 |
| R3 | 私有通道授权与 JWT 过期 | Realtime Authorization · https://supabase.com/docs/guides/realtime/authorization | 在线文档 | 授权 = `realtime.messages` 上的 RLS + `realtime.topic()`；策略在 WebSocket 握手/订阅时校验；**若 Channel 收不到新 JWT，JWT 过期即断连** | 私有通道 | supabase-js 2.98 `dist/index.mjs:361-373`：`_listenForAuthEvents` 在 `TOKEN_REFRESHED`/`SIGNED_IN` 时自动 `realtime.setAuth(token)`；`index.mjs:228-230` 用 `accessToken` 回调初始化；realtime-js `RealtimeClient.js:126-128` 连接时 `_setAuthSafely('connect')` 再取一次 token。**应用层不需要手动 setAuth**。应用是否处理 `CHANNEL_ERROR`/`CLOSED` 状态 **待分区 C 核** | 采纳；社区「必须手动 setAuth」（GitHub supabase-js#1732，2024-01-10 closed not planned）属旧版行为，当前版本已由库处理，但**休眠 >1h 后恢复**仍列待验证 |
| R4 | Realtime 限额（Free） | Realtime Limits · https://supabase.com/docs/guides/realtime/limits | 在线文档 | 并发连接 200、100 msg/s、广播 payload 256KB、每连接 100 channel；超限会被断连并自动重连 | Free tier | 两人产品：每人 2 个 channel（`world:{id}` + `user:{uid}`）| 采纳：无容量风险；payload 上限与 `messages.content ≤4000` 无关 |
| R5 | RLS 性能与安全定义函数 | Row Level Security · https://supabase.com/docs/guides/database/postgres/row-level-security | 在线文档 | `auth.uid()` 包成 `(select auth.uid())` 触发 initPlan 缓存；策略过滤列须建索引（且为 btree 首列）；策略写 `to <role>`；security definer 函数设 `search_path = ''` | Postgres RLS | `ai/features/supabase.md` §三/四：13 条 initplan、4 个 FK 无索引、6 个函数 `search_path` 可变——仍未修（TODO 继承待办） | 采纳为 S-1/S-2 migration 的官方依据；本步不执行（线上不可达） |
| R6 | `auth.getUser()` vs `getSession()` | JS Reference getUser · https://supabase.com/docs/reference/javascript/auth-getuser ；getSession · https://supabase.com/docs/reference/javascript/auth-getsession | 在线文档 | `getUser()` **每次都向 Auth 服务器发网络请求**以验证 JWT；`getSession()` 读本地 storage、必要时刷新，浏览器端 storage 可信时用它即可，服务端才必须用 `getUser()` | 浏览器 SPA | `timeline.md` §五-2 记录的「断网误报未登录」根因就是 `getUser()` 的网络请求失败；`lib/supabase.ts currentUserId()` 用哪一个 **待分区 A/B 核** | 采纳：客户端取当前用户 id 应走 `getSession()`（或 `onAuthStateChange` 缓存），网络错误与未登录分开 |
| R7 | 会话与自动刷新 | Auth Sessions · https://supabase.com/docs/guides/auth/sessions | 在线文档 | 默认 access token 有效期 1 小时 | 默认配置 | 本机 auth-js 2.98 `lib/constants.js:3-10`：每 30s tick，剩余 <3 tick（90s）时刷新；`GoTrueClient.js:2265-2274` 监听 `visibilitychange`，可见时 `_recoverAndRefresh()` | 采纳：应用层无需自建刷新；但 tab 后台被节流 + 休眠超 1h 的恢复路径属待验证 |
| R8 | Storage 签名 URL 契约 | Storage Downloads · https://supabase.com/docs/guides/storage/serving/downloads | 在线文档 | `createSignedUrl(path, expiresIn秒)` → `{ data: { signedUrl } }`；签名用独立内部密钥，与 Auth JWT 无关（JWT 过期不影响已签 URL） | 私有桶 | 本机 storage-js 2.98 `index.d.mts:1091-1097`：`createSignedUrls(paths[], expiresIn, {download?})` 返回**逐项** `{ error, path, signedUrl }`——部分失败是逐项的，调用方须检查每项 `error`；`lib/storage.ts signImageUrls` 是否逐项检查 **待分区 B 核** | 采纳 |
| R9 | React 19 StrictMode 开发期行为 | StrictMode · https://react.dev/reference/react/StrictMode | React 19 | 开发期组件渲染两次、Effect 走 setup→cleanup→setup、ref 回调同样重跑；仅开发期，生产不受影响；Effect 必须幂等且有清理 | 开发期 | `main.tsx` 是否包 `<StrictMode>`、Pixi Application / Realtime 订阅在双跑下是否泄漏 **待分区 A/C/D 核** | 采纳为判据 |
| R10 | react-hooks v7 新规则 | eslint-plugin-react-hooks · https://react.dev/reference/eslint-plugin-react-hooks | v6/v7 recommended | `refs`：不得在渲染期读写 ref；`set-state-in-effect`：不得在 effect 内同步 setState；另有 `immutability` / `purity` / `static-components` 等 | 使用 v7 | 本步基线 eslint：`room-scene.tsx:141` 一条「未使用的 disable 指令」警告；删掉后暴露的 `refs` 错误 **待分区 D 诊断根因** | 采纳为判据 |
| R11 | PixiJS 8 资源生命周期 | Garbage Collection · https://pixijs.com/8.x/guides/concepts/garbage-collection ；Application API · https://pixijs.download/release/docs/app.Application.html | 8.x | `destroy()` 立即释放 GPU 资源；`TextureGCSystem` 默认 3600 帧未用即回收、每 600 帧检查；`texture.unload()` 手动卸载；`Application.destroy(rendererDestroyOptions { removeView }, { children, texture, textureSource, context })` | 8.x | `room-scene.tsx` / `compositor.ts` 的销毁参数与贴图归属 **待分区 D 核** | 采纳为判据 |

未能打开的页面：`/docs/reference/javascript/storage-from-createsignedurls`（404）、`/docs/guides/realtime/broadcast-replay`（404）——改用本机类型定义与博客原文核实（R2、R8）。
