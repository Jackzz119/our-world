# FINDINGS — 当前审核发现

任务状态仍以 `ai/TODO.md` 为准。本表登记审核发现与待采纳建议，不冒充已实施任务；原始记录见各 run 的 `evidence/*-review.md`。
问题 ID 跨复查稳定，所属步骤可变，状态与证据保留。证据等级：**实测** = 本轮命令/脚本实际执行；**静态** = 全文阅读 + grep 引用面；**待验证** = 需线上或运行环境。

## 第一步（运行 2026-09-18-01，基线 3fd52a5）

### 已处理

| ID     | 结论                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 证据 / 定位                                                                                                          |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| PA-001 | 删除 55 个证据充分的冗余文件（约 59.7MB）：2 个违反既有 `.gitignore` 规则却被跟踪的文件（`arts/meshes/scene.blend1`、`__pycache__/*.pyc`）、1 个 0 字节安装缓存 `deps/.lock`、3 个零引用死文件（`timeline_3d_posts.html`、`src/types/database.ts`、`public/mock/couple-feed.json`）、1 个与 `navigation-concepts/` 逐字节相同的目录 `ui-concepts/`（4 文件）、13 张可由 `art-relighting-preview/index.html` 同参数再生成的纹理图、5 张脚本失败分支残留截图、27 个可由 `render-journal-turn.mjs`/`check-journal-turn.mjs` 一条命令再生的翻页选帧与 32 字节结果。每项均 grep 全仓（含 CSS url()、HTML src、manifest、脚本读取侧）确认零消费者；恢复依据 `git checkout 3fd52a5 -- <path>` | 静态 + `git check-ignore` 实测；清单 `runs/2026-09-18-01/evidence/deleted-files.txt`；各分区 review 的「删除候选」节 |
| PA-002 | 根 `README.md` 是 Vite+Supabase「todos」模板残留（引用不存在的 `src/services/todos.service.ts`、`useTodos.ts`），已重写为项目入口（定位、文档索引、技术栈、命令、目录）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `evidence/root-review.md#findings` F-01                                                                              |
| PA-003 | 活文档逐份压缩并核对源码事实：PROJECT/TODO 由审核直接改写，Features 与设计系统文档由独立子任务压缩并附 ledger（保留/删除/改正清单）；逐文档字节与 2/3 验收见阶段报告                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `runs/2026-09-18-01/metrics/documents/`、`evidence/ledger-*.md`                                                      |
| PA-004 | 短文档陈旧陈述修正（不为凑比例裁剪）：`scene.md` 房间模板并无音景字段；`effects.md`「默认静音启动」实为「无手势不自动播放」且音景仅是目标；两份 `production-batches.md`、`visual-principles` 仍以已成桩的 STYLE 为依据；`prompts.md` 参考图路径已迁移；`cinnaglass-history/README.md` 把重复目录列成两份资料；`journal-room-object/codex-report.md` 提案时状态加 2026-09-18 归档说明                                                                                                                                                                                                                                                                                                   | 短文档例外复核结构化结果 `evidence/workflow-structured-results.json`；`evidence/design-review.md#陈旧陈述`           |
| PA-005 | `ai/STYLE.md` / `ai/UX.md` 已是兼容桩，但源码 6 处、文档 2 处按旧章节号 `§2/§4/§5/§6/§8` 引用：桩内补「旧章节 → 现文档」对应表，使引用可回溯；源码注释本身留第二步改                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `evidence/src-review.md` F5；`evidence/ai-core-review.md` F3                                                         |
| PA-006 | PROJECT.md 陈旧事实随压缩改正：标签池实际只有 `chat`（`auth` 未走 Logman）；结构树补全 hooks/utils/pages/journal-\* 并标 `database.ts` 死文件；`types/ ✅ 保留` 与「database.ts 死文件」自相矛盾已消除；`reboot/` 索引补时点说明；`@react-three/rapier` 残留登记                                                                                                                                                                                                                                                                                                                                                                                                                       | `evidence/ai-core-review.md` §2 S1/S10、`evidence/src-review.md` F8                                                  |

### 用户已决定并于 2026-09-19 执行（原「待用户决定」）

用户 2026-09-19 决定：① 3D 源不需要 → 已删 `ai/blender/` 与 `arts/meshes/`；② page-flip 历史链不需要 → 已删 `journal-book.tsx`、`page-flip.d.ts`、`patches/`、`pnpm-workspace.yaml`、`check-journal.mjs` 与依赖，lockfile 已刷新；③ `codex-visual/` 移入 `ai/codex-visual/` 只留 `codex-visual` 技能的 12 个比稿/审核批次，比稿图与设计系统/public 重复的副本删除并改链接，生产用生成批次的已采用原件移到 `arts/`（characters、rooms/thumbs、rooms/study/source 与 generated），失败尝试与被取代轮次删除；`public/` 只留运行时真正读取的文件，4 份 manifest/turntable.json 移到 `arts/`（装配器 `--manifest` 参数改写出路径）；④ 旧验证截图删除 35 张（`_shots` 23、`journal-book-directions/verification` 12），近期（2026-09-07～11）验证素材保留；⑤ 技能目录：项目无 `ai/jaSkill`，以内容更新的一侧为准把 `.claude/skills` 与 `.agents/skills` 对齐（monet / ui-tailor / codex-visual 正文一致，仅 Codex 显示元数据 `agents/openai.yaml` 留在 `.agents`），删除旧 `.claude/skills/monet/design-system.md`；`ai/Features` → `ai/features`，30 个文件的引用同步。字体源 ttf 与 `book-alpha.png` 用户 2026-09-19 决定保留（PA-011 ✅）。

| ID        | 范围 / 结论                                                                                                                                                                                                                                                                                                                | 证据                                                                                        | 尚需什么                                                                                                                                                                                                                                    |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PA-007 ✅ | `ai/blender/scripts/*.py`（3）+ `arts/meshes/{metaspace,avatar,scene}.blend`（1.9MB）：three.js 场景 2026-08-10 退役（`4619fff`），脚本引用的 `metaspace.tsx`、`public/models/*.glb`、`ai/blender/metaspace.md` 均已不存在，脚本写死 `D:\Repo` 路径，全仓零消费者；`scene.blend` 无生成脚本、配套文档已删                  | 静态；`evidence/ai-core-review.md` F7、`evidence/batches-review.md` F-6                     | 用户确认是否为 R2/R3 保留 3D 源作备用。是 → 保留并在 PROJECT 索引标「已退役 3D 试验」；否 → 删除整组（恢复依据 3fd52a5）                                                                                                                    |
| PA-008 ✅ | `page-flip` 依赖 + `patches/page-flip@2.0.7.patch` + `pnpm-workspace.yaml` 条目 + `src/types/page-flip.d.ts` + `src/themes/cinnaglass/journal-book.tsx`（零 importer）+ `scripts/check-journal.mjs`（驱动已退役引擎）：整条链只服务被 `journal-room-book` 取代的旧翻页；`book-implementation.md:27` 明示「保留作历史实现」 | 静态；`evidence/src-review.md` F3、`evidence/root-review.md` F-05/F-07                      | 用户定时点：删则六件一起删并 `pnpm install` 刷 lock、build + 手测翻页；留则 TODO 记带期限清理项                                                                                                                                             |
| PA-009 ✅ | `codex-visual/` ↔ `ai/design_system` / `arts` / `public` 精确重复约 66MB（概念图 6 张、活物分解 3 张、导航比稿 3 张、三档原画 3 张、角色 4 张、头像/唱片封面 3 张、唱片机三批图层 18 文件）。设计系统 2026-09-12 自述「正式副本在本域」、codex-visual 为「原始批次含失败尝试」——两侧都被登记链接                           | 静态 sha256；`evidence/batches-review.md` F-1/F-2/F-4/F-5、`evidence/design-review.md` F-01 | 用户选策略：A 保持双份（现状）；B design_system 侧改链接指向 codex-visual 原件（省 36MB，常驻文档不再自足）；C `arts/rooms/study/generated/` 改定义为「装配输入库」只留装配器实际读取的 3 个文件 + README（省 12MB，见 batches F-2 方案 A） |
| PA-010 ✅ | 验证脚本一次性输出仍留仓约 48MB：`navigation-verification` 23 张状态/视口图、`book-verification` 8 张、`journal-book-directions/verification` 13 张已否决方案回归图、`cinnaglass-history/_shots` 23 张零引用截图                                                                                                           | 静态；`evidence/design-review.md` D-05/D-07/D-08                                            | 用户决定保留几张作视觉存档还是按清单删除；建议每档留 1 张 scene 图                                                                                                                                                                          |
| PA-011 ✅ | `arts/ui/journal/LXGWWenKaiLite-Regular.ttf`（13.9MB 第三方字体源，运行时只用 woff2，manifest 已记 upstream URL + sha）；`arts/ui/journal/book-alpha.png`（打包器每次重写的中间产物）                                                                                                                                      | 静态；`evidence/batches-review.md` F-7                                                      | 用户决定保留：ttf 作离线重转源，`book-alpha.png` 作打包中间件                                                                                                                                                                               |
| PA-012 ✅ | `.claude/skills/` 与 `.agents/skills/` 两套技能目录内容已漂移（codex-visual、monet 各 3 文件不同；`ui-tailor` 只在 `.agents`；`monet/design-system.md` 只在 `.claude`），`AGENTS.md` 规定不复制成两套真源                                                                                                                  | `evidence/skills-dir-drift.txt`（目录级 diff，未读正文）                                    | 受保护范围，须经 `skill-creator` 且用户同意后处理                                                                                                                                                                                           |
| PA-013 ✅ | `ai/` 子目录命名三风格并存（`Features` PascalCase、`design_system` snake、其余 kebab/小写）；协议文件示例即用 `ai/Features/`，引用面 25+ 处                                                                                                                                                                                | `evidence/ai-core-review.md` F9                                                             | 建议不重命名、新目录统一 kebab-case；用户确认是否接受长期不一致                                                                                                                                                                             |

### 待第二步或后续步骤处理

| ID        | 结论                                                                                                                                                                                                                                                                                   | 后续动作                                                                                                                                                                                                   |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PA-014    | `package.json` 残留 `@react-three/rapier`（src 零引用）与 `vite.config.ts` 的 `optimizeDeps` 段/drei 注释是 three 退役漏删；lockfile 因此仍锁 three/fiber                                                                                                                              | 第二步：移除依赖与配置块，`pnpm install && pnpm build` 验证；TODO 退役清理已登记                                                                                                                           |
| PA-015 ✅ | 源码注释 20 余处引用失效文档/编号：`ai/UX.md §N`、`ai/STYLE.md §6`（6 处）、`ai/Features/channel.md`/`world.md`（5 处）、`chat.md CH-12/CH-17/EMO-3/EMO-4`、`ux decisions D-7-3`、`ow.css`；`sql/dev-create-world.sql:9` 引用不存在的 `ai/Features/world.md R-6`（应为 `supabase.md`） | 第二步注释精简时按 `evidence/src-review.md` F5 表逐条改指向                                                                                                                                                |
| PA-016 ✅ | 既有构建阻断：`shell/chat-card.tsx:10` 从 `../model` 导入不存在的 `Msg`（实际在 `chat-data.ts`），`tsc -b` 失败；`music.tsx:9` react-refresh lint error                                                                                                                                | 第二步一行修复；TODO Bugs 已有                                                                                                                                                                             |
| PA-017 ◐  | `scripts/` 12 个脚本无一登记于 `package.json`，9 个 `.mjs` 依赖 playwright/sharp/pngjs 借用外部 `DIARY_NODE_MODULES`（3 个无回退硬依赖），2 个 `.py` 无 requirements，默认端口 5175 与 dev 5173/5174 不一致；唯一零依赖可运行的是 `check-design-system.mjs`                            | 第二步已做：`scripts/lib/deps.mjs` 统一依赖解析与 `baseUrl()`（默认 5173，`JOURNAL_URL` 覆盖）；依赖策略写入 `CONVENTIONS.md`（不进 package.json）。未做：`check:design` npm script 与脚本登记表（低优先） |
| PA-018    | `ui-system.html` 组件表仍描述已否决的 B 苔绿日记几何、16px/1.75 正文、`diary.css` 现役等（S-05～S-08、S-13、S-14）；`decisions-v2.md` 白纸形态被两处活引用当依据未标替代                                                                                                               | UI Tailor 在 M1 前修正预览页陈旧项并在引用处加「形态以 book-implementation.md 为准」                                                                                                                       |
| PA-019    | `ux-decisions.md:9/24/37` 反引号路径指向不存在的 `ai/Features/ui-system.md`、`channel.md`、`settings.md`；`ux-decisions.md` 章节顺序 D-10→D-12→D-11                                                                                                                                    | 历史归档原件，只在文首加阅读说明，不改正文；本轮未动                                                                                                                                                       |
| PA-020    | `public/` 内 4 份 JSON（3 manifest + `turntable.json`）是溯源文档，随 build 发布（<4KB）；`public/vite.svg` 模板 logo 仍作 favicon                                                                                                                                                     | 低优先：迁到 `arts/` 需同步 manifest/设计文档/monet skill 链接；favicon 待美术产出后替换                                                                                                                   |
| PA-021    | `AGENTS.md:11` 要求按需读取不存在的 `ai/UNITY_PROJECT.md`、`ai/UNITY_TODO.md`；触发表中的 `skill-creator`/`skill-installer` 在 `.claude/skills/` 无同名目录（本会话以插件形式存在）                                                                                                    | 受保护协议，仅登记                                                                                                                                                                                         |
| PA-022    | `inventory.py` 因 `SKIP_NAMES` 含 `__pycache__` 漏收 1 个已跟踪 `.pyc`（本轮已删）；下轮清单应对「被 git 跟踪但被忽略规则命中」的文件单独列出                                                                                                                                          | 第五步/下轮脚本迭代                                                                                                                                                                                        |

已在 `ai/TODO.md` Bugs 与 `ai/Features/ui-system/audit.md` 登记、不重复开 ID 的线索：世界设置有真实保存链但无 UI 入口；`WorldPage` widget 键与 `rail.tsx` MODULE_DEFS 不一致；`calendar.tsx` `ANNIV` 硬编码与 `world.anniversary` 双真源；`rooms.ts`/`scene.tsx` 待迁出消费者后清理；`diary.css` 中仅被 `journal-book.tsx` 使用的死样式。

## 第二步（运行 2026-09-19-01，基线 a9f7cfa → commits dc74f51 / f3a0ad0 / e656ac9 / eba7dd2）

### 已处理

| ID     | 结论                                                                                                                                                                                                                                                                                                                                                                              | 证据                                                     |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| PA-023 | 同语义重复收口：localStorage 读写 4 份 → `lib/local-store.ts`；`currentUserId` 6 处 → `lib/supabase.ts`；签名续签周期由 TTL 派生；`applyThumbUrls` 两份 → `journal-layout.ts`；`Mood` = `RoomMood`；脚本 `dependency()` 三份 → `scripts/lib/deps.mjs`。AST 同体候选 3 → 0                                                                                                         | `metrics/after/structure-summary.json`；commit `dc74f51` |
| PA-024 | 死代码/死配置：`rooms.ts`（loader 迁出后整文件零消费者）、`Room`/`WidgetPos`/`moodFromHour`、`Tweaks.hudLayout/density`、`uploadMemoryImage` 死参数、`ReactionRow.channel_id` 幻列、`.rooms-tip` 死对；零消费者样式约 190 行（`diary.css` 11 块、`ScreenStyles` 31 条、`cinnaglass.css` 侧栏/浮动动画块、`journal-room.css` 变量），构建 CSS 选择器集合 diff 证明只少了所删选择器 | `evidence/*-apply-log.md`；commits `f3a0ad0`、`eba7dd2`  |
| PA-025 | 注释：无紧邻注释的具名声明 293 → 155（余下多为匿名回调式声明与 JSX 内部函数）；删除 ChatDock/sidebar/R3F/旧椭圆方案等过时叙述；`verify-comment-only.mjs` 证明 44/49 个改动文件只动了注释，其余 5 个的非注释差异逐一核对（类型改动 1、死样式 1、模板字符串内 CSS 注释 3）                                                                                                          | `metrics/comment-only-check.json`；commit `f3a0ad0`      |
| PA-026 | 全局格式化：新建 `.prettierignore`（依赖/锁文件/受保护协议与技能/冻结原件/审核证据/素材树/工具元数据），`prettier --check .` 192 → 0；受保护与冻结路径零改动                                                                                                                                                                                                                      | commit `e656ac9`；`metrics/prettier-write.log`           |
| PA-027 | 结构规范首次成文 `CONVENTIONS.md`（分层与依赖方向、命名、复用/拆分原则、注释、格式化、脚本依赖），标明已采纳与待讨论                                                                                                                                                                                                                                                              | `ai/project-audit/CONVENTIONS.md`                        |

### 用户 2026-09-19 批准并已执行的重构（commits `e674a96`、`ff96938`、第三阶段归位 commit）

执行记录：`evidence/room-refactor-log.md`（帧像素逐字节相同 ×10）、`journal-refactor-log.md`（计算样式 dump 差异 0，B2 只删被证明覆盖的 80 条声明）、`chat-refactor-log.md`（58 条纯函数断言）、`shell-refactor-log.md`（99×56034 与 30×16980 属性差异 0）。**运行时冒烟未做**（本机无 `.env.local`，进不了世界），见各日志「未验证」。PA-033 中已做：`scene.tsx` → `login-backdrop.tsx`、`.cg-panel` 原子类、reset 合并、`Weather.kind` 收窄、import 路径统一、目录归位与改名；**未做**：`image-slot.js` 换 React（丢用户数据与交互，需产品决定）、`world-settings` 入口（产品决定）、`savePw` 接后端（功能，应记 Bug）。

| ID        | 方案                                                                                                                                                                                                                                                                                                                                                                                                                            | 规模 / 风险         | 出处                                                        |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------- |
| PA-028 ✅ | `pixi-scene.ts` `buildScene`（782 行、9 个职责、3 处 TDZ 边缘、2 处同名遮蔽）拆为 textures / homography / lighting / fade-queue / rain / clock / character / affordance / turntable 九模块，8 批提交，唱片机一批最险；验证靠冻结帧像素比对 + 定种随机状态日志 + 贴图收支计数                                                                                                                                                    | 高（无测试）        | `evidence/room-structure-review.md` §2                      |
| PA-029 ✅ | `screens.tsx`（1094 行）拆为 8 个模块 + `ScreenStyles` 迁 CSS；`diary.css` 扁平化（已否决的苔绿方案仍是棕皮书的基础层，改一个值要读两份 700 行文件）——**日记本本 session 冻结，须用户点头**；`journal-*` 七文件移入 `journal/` 子目录（2 个校验脚本硬编码模块 URL 需同步）                                                                                                                                                      | 中～高              | `evidence/journal-screens-structure-review.md` §2、§7.4、B2 |
| PA-030 ✅ | `useChatThreads`（605 行、6 个变化原因）拆 7 文件 5 批，`WorldPage` 返回面契约不变；`ChannelScreen`（428 行、203 行非 UI 代码）拆 6 文件；聊天族建 `chat/` 子目录；`EmoteView` 类型从 UI 移回数据层消除唯一逆向依赖                                                                                                                                                                                                             | 高 / 中 / 低        | `evidence/chat-structure-review.md` §3、§7.1                |
| PA-031 ✅ | `WorldPage`（465 行、扇出 22）按弹窗编排/热点路由/天气实况/聊天气泡/presence 占位拆 hook 与子组件，6 批                                                                                                                                                                                                                                                                                                                         | 中高                | `evidence/shell-structure-review.md` §2                     |
| PA-032 ✅ | **纪念日三份真源**：`calendar.tsx` 硬编码 `ANNIV` + `profile.ts` 兜底 + DB `world.anniversary`；`profile.ts` 正确的日期函数零消费者；后果是真 bug——`WorldPage` 未给日历传 `anniv`，改世界设置后日历卡片不更新。修法：删硬编码、`CalendarScreen` 加 `anniv` prop、两处共用 `daysSince`/`daysUntilAnniversary`；需处理空值                                                                                                        | 中（改接口 + 行为） | `evidence/shell-structure-review.md` B1                     |
| PA-033 ◐  | 其余低收益或需口径的项：`scene.tsx` → `login-backdrop.tsx` 改名；`.cg-panel` 玻璃原子类抽取（需 Monet/UI Tailor 口径）；`index.css` 与 `cinnaglass.css` 两份 reset 合并；`Weather.kind` 收窄为联合类型；import 路径写法统一（69 处）；`image-slot.js` 换 React 头像选择器（丢 reframe 交互与旧 localStorage 数据）；`world-settings` 弹窗无 UI 入口（产品决定）；`settings.tsx savePw` 只显示成功不调后端（功能缺口，应记 Bug） | 低～中              | 四份 review 的 B 类表                                       |

不做并登记理由：不合并 `WeatherTweak` 与 `RoomWeather`；登录页不复用 Pixi 房间；不把九个引擎模块再拆 build/update/dispose；不拆 `friends-page`/`emote-picker`/`emoji-data`；不引入 `supabase gen types`；不把 `scripts/` 纳入 `tsc`；不把 playwright/sharp 加进 `package.json`；不删 `icons.tsx` 零消费者图标；不开 `allowConstantExport`。

## 第三步（运行 2026-09-19-02，基线 f96fc63；只交付诊断，未改源码）

全部为**静态**证据（本机 dev server 未开、Supabase MCP 401、Edge Function 源码不在仓库），线上 DDL/RLS/触发器相关一律标「待核线上」。分区原始记录与 P2/P3 全文：`runs/2026-09-19-02/evidence/03-{auth-session,memories,chat,room-shell}-review.md`（编号 A-/B-/C-/D-）。P1 条目均由审核对照源码二次复核。

### P1（用户可见的功能失败、误导或数据不一致）

| ID     | 范围 / 结论（定位）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 建议、代价与验收                                                                                                                                                                                                                                                                                              | 待办                                                                                             |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| PA-034 | **`currentUserId()` 走 `auth.getUser()` 网络往返并忽略 `error`**（`src/lib/supabase.ts:11-13`）：断网时 auth-js 2.98 返回 `{user:null, error}`，六个数据模块（worlds/posts/chat/emotes/friends）一律抛「未登录…」，大厅与发帖把网络故障说成未登录；每次写操作多一次 `/auth/v1/user` 往返。A-02 / B-01 / C-15 根因                                                                                                                                                                                                                                                                       | 改 `getSession()`（浏览器端 storage 可信，官方 getUser 只在服务端必需，见 `evidence/03-research.md` R6）；`error` 非空抛网络类错误。一处改动。验收：离线进 `/` → 文案为网络类；`getMyWorld` 不再请求 `/auth/v1/user`                                                                                          | **已修 2026-09-19**（`lib/supabase.ts` 改 `getSession()`，网络类错误单独文案；运行时待用户实测） |
| PA-035 | **账号设置整组假反馈**：退出忽略 `signOut()` 返回的 `{error}`，断网/5xx 时本地会话不清、「正在退出…」永久卡死（`settings.tsx:223-237`，auth-js `_signOut` 非 401/403/404 不删会话）；改密从不调 `updateUser`、邮箱行编辑的是 localStorage 假值 `us@ourworld.love`、应用锁无人执行、「上次更新于 3 个月前」硬编码（A-01 / A-03）                                                                                                                                                                                                                                                         | 读 `{error}` 并回滚 `loggingOut` 与 `ow-explicit-logout`，失败可再点；改密接 `updateUser({password})`；邮箱改只读展示 `user.email`；应用锁未设计前删行。≤40 行。验收：离线退出有提示可重试；新密码能登录                                                                                                      | `ai/TODO.md` Bugs「保存新密码是假成功」（扩展范围）                                              |
| PA-036 | **昵称编辑落 localStorage，而界面 DB 优先显示** → 改了看不见；`profiles.display_name` 全仓只读（`settings.tsx:264-279`、`WorldPage.tsx:192-198`、`profiles.ts` 仅 select）；「她」的昵称由我本地改写无意义（A-04）                                                                                                                                                                                                                                                                                                                                                                      | `profiles.ts` 增 `updateMyDisplayName`（依赖 profiles update RLS，待核线上）；「我」写回并同步 `memberNames`，「她」只读。验收：改名后房间/聊天/对方端一致                                                                                                                                                    | TODO 继承待办「昵称编辑写回 display_name」                                                       |
| PA-037 | **世界设置弹窗不可达**：全仓无 `open('world-settings')`（仅 `world-surfaces.tsx:87` 判定），`updateWorld`/`uploadWorldIcon` 整链死代码；纪念日无法设置 → 纪念卡不渲染、日历回退假日期 `2025-06-04`（A-05 / D-09）                                                                                                                                                                                                                                                                                                                                                                       | 设置弹窗加「世界设置 ›」，`WorldPage` 接住 `useSurfaceRouter` 的 `open`；null 纪念日显示「还没设纪念日」而非假数。验收：INDEX 运行时验收清单第五项                                                                                                                                                            | `ai/TODO.md` Bugs「世界设置弹窗没有入口」                                                        |
| PA-038 | **聊天在世界内的呈现**：头顶气泡定时器随任何 `threads` 重算被 cleanup 清掉且不重设，对方气泡挂住直到下一条（`shell/use-world-chat-bubble.ts:28-40`，deps 是整个 `lastMsg` 对象）；rail 红点只看「最后一条是对方的」，无视已读游标，关窗即回亮（同文件 `:18-23`）（C-01 / C-02）                                                                                                                                                                                                                                                                                                         | deps 改标量 `[lastMsg?.id, from, pending]` + cleanup 清 bubble；`unread` 加 `reads[worldConvId][uid]` 比较。≈10 行。验收：4.5s 内回一句气泡仍到点消失；读完关窗红点不回弹                                                                                                                                     | 新增 TODO Bugs                                                                                   |
| PA-039 | **聊天一致性**：重连补拉是「最新 50 条 ∪ 本地」（`store.ts:85-89 mergeRows`、`upsertReactions` 只增），断线期间对方的删除/撤 reaction 永远补不回；编辑/删除/reaction/已读写失败只打 log、本地不回滚、用户无提示（`use-optimistic-send.ts:155-231`）；重试已落库的消息撞 23505 永远卡 failed（C-03 / C-04 / C-06）                                                                                                                                                                                                                                                                       | 补拉按服务端页覆盖同区间（保留 pending/failed）+ `getReactions` 整体替换；四类写失败回滚 + 一行提示；23505 视为成功。≈50 行纯函数，可续第二步断言脚本。验收：双端断网删消息/撤 reaction → 恢复后一致；PATCH 离线 → 内容回弹                                                                                   | TODO「聊天双端联调残项」验收清单追加                                                             |
| PA-040 | **上传管线**：Composer 按 `image/*` 放行、无 25MB 预检，gif/heic 到桶才被拒并把英文原文给用户（`composer.tsx:67,202`；桶只收 png/jpeg/webp/avif）；多图部分成功后失败留 Storage 孤儿，`storage.ts` 无 `remove()`，重试整批重传（`composer.tsx:109-124`）；世界 icon 同型（A-16）（B-02 / B-03）                                                                                                                                                                                                                                                                                         | 用 `EXT_BY_TYPE` + 大小预检并中文提示；`publish` 记已传路径，失败 best-effort `remove`、重试跳过。≈25 行。验收：拖 gif+30MB → 两条中文提示不发请求；第 2 次 upload 改 503 → 桶无残留                                                                                                                          | TODO 继承待办「回忆链路边界测试」                                                                |
| PA-041 | **房间引擎装载期四处**（`room-scene.tsx:81-137`、`compositor.ts:226-227`）：① HiDPI 下 cover-fit 把 `renderer.width`（v8 已是逻辑 px，`ViewSystem.resize` 令 `screen.width = texture.frame.width`）再除 `resolution`，DPR>1 房间缩到左上角露底（第二步像素比对在 DPR=1 看不出）；② 装载期间到达的 mood/天气被丢（`[mood]` effect 在 `sceneRef` 为 null 时跳过，就绪后不回填）；③ 任一贴图 404 → `Assets.load` reject，IIFE 无 catch，整间房静默空白；④ 装载中卸载：`app = a` 在 `buildScene` 前，cleanup 已 `destroy`，`buildScene` 继续 `app.stage.addChild` → TypeError（D-01～D-04） | ① 改 `renderer.screen.width/height`（一行）；② 场景就绪后 `setMood/setWeather(ref.current,false)`；③ try/catch + `onSceneError`，零件用 `strategy:'skip'` 降级；④ `buildScene` 接受取消信号。验收：DPR 1/1.25/2 根 scale 一致；限速下 auto 雨天出场即下雨；改名一张 PNG 有错误态；进世界 100ms 内回大厅无报错 | 新增 TODO Bugs                                                                                   |
| PA-042 | **dev 凭据可进生产包**：`VITE_DEV`/`VITE_DEV_EMAIL`/`VITE_DEV_PASSWORD` 只经 `getEnvFlag` 判断，未与 `import.meta.env.DEV` 联动（`ProtectedRoute.tsx:15-19`）；`vite build` 同样读 `.env.local`，**本机（Windows）实际存在 `.env.local`**，构建即内联真实密码（A-13）                                                                                                                                                                                                                                                                                                                   | `const DEV_LOGIN = import.meta.env.DEV && getEnvFlag('VITE_DEV')`；构建后 `grep -r VITE_DEV_PASSWORD dist` 为空。一行。验收：`pnpm build` 后 dist 无凭据                                                                                                                                                      | **已修 2026-09-19**（`utils/index.ts` 静态 env 表 + `DEV` 门控；打包后实测密码/邮箱值消失）      |

### P2 / P3（边界、维护性、文档；实施与否待用户裁决）

| ID     | 范围 / 结论                                                                                                                                                                                                                                                                                                                                                                                                                              | 建议                                                                                                                                                                                                   |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PA-043 | **错误文案直出英文原文**：登录/找回/大厅/发帖/聊天/贴纸六类出口各自 `err.message` 直出，`feed.error` 甚至从未显示（A-10 / A-11 / B-05 / B-06 / C-15）；三个分区各自提出一个映射 helper                                                                                                                                                                                                                                                   | 只建一个零 React 的 `src/lib/user-error.ts`：按 `AuthError.code` / PostgREST `code` / Storage `statusCode` → 中文 + `retryable`，必保 `raw`；≈40 行，6 个调用点各改一行。不做 toast 系统、不做重试策略 |
| PA-044 | **签名 URL 续签三份实现**：缩略图 `use-signed-thumbs.ts` 有定时 + 重可见；世界 icon（`useWorldSession.ts:16`）与贴纸（`use-emote-library.ts:65`）硬编码 `40*60*1000`、无 `visibilitychange`；`PROJECT.md` 数据库节「tab 重可见重签」对后两者不成立（A-08 / C-11；PA-023 只做了一半）                                                                                                                                                     | 泛化为 `useSignedUrls(paths)` 三处共用；PROJECT.md 措辞已在本步修正                                                                                                                                    |
| PA-045 | **Realtime 流层**：`use-world-stream` 与 `use-account-stream` 逐字重复 12 行 status 处理；无 `connected` 状态给 UI（离线零感知）；`channels` 表无事件类型（世界内新建频道不实时出现）；同 topic「离开中」channel 无防护（C-07 / C-14）。**JWT 续期不需应用处理**：supabase-js 2.98 在 `TOKEN_REFRESHED/SIGNED_IN` 自动 `realtime.setAuth`（`dist/index.mjs:361-373`）；Broadcast Replay（`config.broadcast.replay`，≤25 条）可作断线补充 | `useTopicStream(topic, load, onEvent)` 收口 + `connected`；PA-039 的补拉语义放这里；可选 `replay.since`                                                                                                |
| PA-046 | **异步 effect 无取消 / StrictMode 双发**：首屏 `get_feed_posts` 双发、无 AbortController（B-14）；`loadOlder` 无世界守卫、与 `reload` 竞态（B-07）；成员档案 effect 依赖 `world` 对象身份每次保存都重拉（A-09）；dev 自动登录 StrictMode 双发且 `.finally` 无 catch（A-13）                                                                                                                                                              | 各处加 `cancelled` 旗标或 AbortSignal；依赖改 `world.id`                                                                                                                                               |
| PA-047 | **输入校验规则不一致**：消息长度大窗 500 / 窄卡无 / 编辑无 vs DB 4000（待核）；密码找回 ≥6 / 设置 ≥4 / 注册无；`posts.content` 无上限（C-05 / A-12）                                                                                                                                                                                                                                                                                     | 常量集中（`lib/limits.ts`），DB 侧 check 待核后对齐                                                                                                                                                    |
| PA-048 | **呈现层契约**：Enter 闸门只排除 INPUT/TEXTAREA，聚焦按钮/contenteditable 时回车会开聊天（C-13 / D-11）；widget 注册表 5 个死键 + `presence` 开关无消费者（D-10，第一步已登记）；z-index 无单一阶梯，浮窗高于弹窗 scrim（D-15）；solo 世界渲染假伴侣「小满 · 在线」（A-18，产品口径）；resize 首帧旧布局（D-16）                                                                                                                         | 闸门改「无可编辑焦点」判定；z-index 阶梯写进 `ui-system.md`；假伴侣待产品定                                                                                                                            |
| PA-049 | **壳层健壮性与性能线索（移交第四步）**：无兜底路由 / ErrorBoundary，`getEnv` 缺失即白屏（A-17）；`useLiveClock` 挂在 `WorldPage` 根，整个壳层 1 Hz 重渲染（A-14）；30fps 常渲染且 start/stop 三个互不知情的开关（D-07）；晴天也全屏跑 `AdjustmentFilter`（D-06）；每次切氛围/天气泄漏 2–3 张离屏画布贴图（D-05）                                                                                                                         | ErrorBoundary + `*` 路由本步可做；其余作为第四步 profiler 的假设清单，先测再改                                                                                                                         |
| PA-050 | **脚本与文档未随第二步改名同步**：`check-journal-art.mjs:231` / `check-journal-turn.mjs:377` 断言样式表名 `journal-room.css`/`journal-turn.css`（现为 `journal/room.css`、`journal/turn.css`）必假；`check-diary.mjs` 依赖 `.tl-card/.tt/.tl-time` 已零命中；≈20 处模块头仍写旧文件名；`timeline.md` 的 `slot-change` 事件已不存在；`arts/rooms/study/generated/README.md` 重跑命令缺必填 `--review`（B-17 / B-18 / C-18 / A-19 / D-13） | 脚本断言改名（两处各一行）、`check-diary.mjs` 退役或重写；注释与文档批量校正                                                                                                                           |
| PA-051 | **隐私备查**：默认 `weather:'auto'` 把浏览器定位经纬度发给第三方 `api.open-meteo.com`，除定位弹窗外无应用内说明（A-20）                                                                                                                                                                                                                                                                                                                  | 设置页一句说明或默认改 `sun`；由用户定                                                                                                                                                                 |
| PA-052 | **CSS 卫生（含冻结区）**：死变量/死块/重复字面量（D-14）；`.tl` 类名碰撞被「刻意保留」，纸角 `journal-photo-corner tl` 吃到时间线死样式（B-15）；`diary.css` 死规则与死变量（B-16，🧊 仅建议）                                                                                                                                                                                                                                           | 冻结解除后一并清                                                                                                                                                                                       |
| PA-053 | **冻结区 DM 侧**：`find_profile_by_email` 是邮箱存在性探针（白名单靠 Dashboard 关注册兜底，风险低）（C-16）；页面不可见时也上报已读、DM 显示假「已读」（C-12）                                                                                                                                                                                                                                                                           | DM 去留拍板后处理                                                                                                                                                                                      |

本步核实为**不成立或已由库覆盖**、不开 ID：Realtime JWT 续期需手动 `setAuth`（旧版行为）；StrictMode 双订阅/双 Application（`room-scene.tsx` 与两 stream 均有 `disposed/cancelled` 守卫）；`room-scene.tsx:141` 的 eslint-disable 删掉会报错（HEAD 实测 unused，渲染期改 ref 的代码存在但 `refs` 规则未抓，D-19 记为卫生项）；echo 去重、`convsFor` 单点、窄卡只渲染首会话、三面常驻、Esc 层级、ST-U/V 草稿规则、缩略图续签——均与文档一致。
