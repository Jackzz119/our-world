# Claude Code / Codex 桌面版新建 worktree 时，把这些 gitignored 文件从主检出复制进去（gitignore 语法）。
# git 自己不读这个文件；Multica、Codex CLI --worktree、手动 git worktree add 都不会复制——
# 那些路径靠 bash ai/jaSkills/custom-skill/scripts/sync-worktree.sh 保底（没有主检出时退到 ai/jaAgents/ 的入库副本）。
/CLAUDE.md
/AGENTS.md
